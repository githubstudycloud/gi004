# Spring Boot 4.0 模块化架构详解

## 一、模块化演进

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          模块化演进历程                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   Spring Boot 1.0 (2014)          Spring Boot 3.5          Spring Boot 4.0  │
│                                                                             │
│   ┌─────────────────┐            ┌─────────────────┐      ┌──┬──┬──┬──┐    │
│   │                 │            │                 │      │  │  │  │  │    │
│   │  autoconfigure  │            │  autoconfigure  │      ├──┼──┼──┼──┤    │
│   │     182 KiB     │     ──►    │      2 MiB      │  ──► │  │  │  │  │    │
│   │                 │            │                 │      ├──┼──┼──┼──┤    │
│   └─────────────────┘            └─────────────────┘      │  │  │  │  │    │
│                                                           └──┴──┴──┴──┘    │
│       单体 JAR                      臃肿的单体               70+ 独立模块    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 二、模块化的优势

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          模块化优势                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────┐    ┌───────────────────────────┐            │
│  │      🚀 启动更快          │    │      💾 内存更省           │            │
│  │                           │    │                           │            │
│  │   只加载需要的模块         │    │   更小的类路径             │            │
│  │   减少类扫描时间          │    │   更少的元数据加载          │            │
│  │   更快的自动配置          │    │   减少 Metaspace 占用       │            │
│  └───────────────────────────┘    └───────────────────────────┘            │
│                                                                             │
│  ┌───────────────────────────┐    ┌───────────────────────────┐            │
│  │      🔍 易于调试          │    │      📦 更清晰的依赖       │            │
│  │                           │    │                           │            │
│  │   类路径噪音减少          │    │   模块边界成为契约          │            │
│  │   IDE 代码补全更精准       │    │   依赖关系更明确            │            │
│  │   问题定位更容易          │    │   避免不必要的传递依赖       │            │
│  └───────────────────────────┘    └───────────────────────────┘            │
│                                                                             │
│  ┌───────────────────────────┐    ┌───────────────────────────┐            │
│  │      🛡️ 更好的隔离        │    │      🔧 可维护性          │            │
│  │                           │    │                           │            │
│  │   技术模块互不干扰        │    │   团队可独立开发模块        │            │
│  │   升级影响范围可控        │    │   代码边界清晰              │            │
│  │   安全漏洞隔离           │    │   测试更加聚焦              │            │
│  └───────────────────────────┘    └───────────────────────────┘            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 三、70+ 模块分类

### 3.1 核心模块

```
┌─────────────────────────────────────────────────────────────────┐
│                        核心模块                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-core                                               │
│  ├── spring-boot                    # Boot 核心                 │
│  ├── spring-boot-autoconfigure      # 自动配置基础              │
│  ├── spring-boot-actuator           # 监控端点                  │
│  ├── spring-boot-devtools           # 开发工具                  │
│  ├── spring-boot-loader             # JAR 加载器               │
│  ├── spring-boot-properties-migrator# 属性迁移器               │
│  └── spring-boot-test               # 测试基础                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Web 模块

```
┌─────────────────────────────────────────────────────────────────┐
│                         Web 模块                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-web                                                │
│  ├── spring-boot-webmvc             # MVC 框架 (替代 starter-web)│
│  ├── spring-boot-webmvc-test        # MVC 测试                  │
│  ├── spring-boot-webflux            # WebFlux 响应式            │
│  ├── spring-boot-webflux-test       # WebFlux 测试              │
│  ├── spring-boot-jersey             # JAX-RS 支持               │
│  ├── spring-boot-jersey-test        # Jersey 测试               │
│  ├── spring-boot-graphql            # GraphQL                   │
│  ├── spring-boot-graphql-test       # GraphQL 测试              │
│  ├── spring-boot-hateoas            # HATEOAS                   │
│  ├── spring-boot-restdocs           # REST Docs                 │
│  ├── spring-boot-websocket          # WebSocket                 │
│  ├── spring-boot-rsocket            # RSocket                   │
│  └── spring-boot-restclient         # HTTP 客户端               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 数据访问模块

```
┌─────────────────────────────────────────────────────────────────┐
│                       数据访问模块                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-data                                               │
│  │                                                              │
│  │  ─── 关系型数据库 ───                                        │
│  ├── spring-boot-data-jpa           # JPA (Hibernate)           │
│  ├── spring-boot-data-jpa-test      # JPA 测试                  │
│  ├── spring-boot-data-jdbc          # JDBC                      │
│  ├── spring-boot-data-jdbc-test     # JDBC 测试                 │
│  ├── spring-boot-data-r2dbc         # R2DBC 响应式              │
│  ├── spring-boot-data-r2dbc-test    # R2DBC 测试                │
│  │                                                              │
│  │  ─── NoSQL 数据库 ───                                        │
│  ├── spring-boot-data-mongodb       # MongoDB                   │
│  ├── spring-boot-data-mongodb-reactive  # MongoDB 响应式        │
│  ├── spring-boot-data-redis         # Redis                     │
│  ├── spring-boot-data-redis-reactive    # Redis 响应式          │
│  ├── spring-boot-data-elasticsearch # Elasticsearch             │
│  ├── spring-boot-data-cassandra     # Cassandra                 │
│  ├── spring-boot-data-cassandra-reactive # Cassandra 响应式     │
│  ├── spring-boot-data-neo4j         # Neo4j 图数据库            │
│  ├── spring-boot-data-couchbase     # Couchbase                 │
│  │                                                              │
│  │  ─── 其他 ───                                                │
│  ├── spring-boot-data-ldap          # LDAP                      │
│  ├── spring-boot-data-rest          # Data REST                 │
│  └── spring-boot-flyway             # 数据库迁移                │
│  └── spring-boot-liquibase          # 数据库迁移                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4 消息模块

```
┌─────────────────────────────────────────────────────────────────┐
│                        消息模块                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-messaging                                          │
│  ├── spring-boot-amqp               # RabbitMQ                  │
│  ├── spring-boot-amqp-test          # AMQP 测试                 │
│  ├── spring-boot-kafka              # Apache Kafka              │
│  ├── spring-boot-kafka-test         # Kafka 测试                │
│  ├── spring-boot-pulsar             # Apache Pulsar             │
│  ├── spring-boot-pulsar-test        # Pulsar 测试               │
│  ├── spring-boot-pulsar-reactive    # Pulsar 响应式             │
│  ├── spring-boot-artemis            # ActiveMQ Artemis          │
│  ├── spring-boot-jms                # JMS                       │
│  └── spring-boot-integration        # Spring Integration        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.5 安全模块

```
┌─────────────────────────────────────────────────────────────────┐
│                        安全模块                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-security                                           │
│  ├── spring-boot-security           # Security 核心             │
│  ├── spring-boot-security-test      # Security 测试             │
│  ├── spring-boot-oauth2-client      # OAuth2 客户端             │
│  ├── spring-boot-oauth2-resource-server # OAuth2 资源服务器     │
│  ├── spring-boot-oauth2-authorization-server # 授权服务器       │
│  ├── spring-boot-ldap               # LDAP 安全                 │
│  └── spring-boot-session            # Session 管理              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.6 可观测性模块

```
┌─────────────────────────────────────────────────────────────────┐
│                      可观测性模块                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-observability                                      │
│  ├── spring-boot-actuator           # Actuator 核心             │
│  ├── spring-boot-actuator-test      # Actuator 测试             │
│  ├── spring-boot-opentelemetry      # OpenTelemetry (新增)      │
│  ├── spring-boot-micrometer         # Micrometer                │
│  ├── spring-boot-micrometer-tracing # 分布式追踪                │
│  ├── spring-boot-prometheus         # Prometheus                │
│  ├── spring-boot-zipkin             # Zipkin                    │
│  └── spring-boot-wavefront          # Wavefront                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.7 批处理/任务模块

```
┌─────────────────────────────────────────────────────────────────┐
│                     批处理/任务模块                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-batch                                              │
│  ├── spring-boot-batch              # Spring Batch              │
│  ├── spring-boot-batch-test         # Batch 测试                │
│  ├── spring-boot-quartz             # Quartz 调度               │
│  └── spring-boot-task               # Task 执行                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.8 序列化/JSON 模块

```
┌─────────────────────────────────────────────────────────────────┐
│                     序列化/JSON 模块                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-json                                               │
│  ├── spring-boot-jackson            # Jackson 3.0              │
│  ├── spring-boot-gson               # Gson                      │
│  ├── spring-boot-jsonb              # JSON-B                    │
│  └── spring-boot-kotlin-serialization # Kotlin 序列化 (新增)    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.9 模板引擎模块

```
┌─────────────────────────────────────────────────────────────────┐
│                     模板引擎模块                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-template                                           │
│  ├── spring-boot-thymeleaf          # Thymeleaf                 │
│  ├── spring-boot-freemarker         # FreeMarker                │
│  ├── spring-boot-mustache           # Mustache                  │
│  └── spring-boot-groovy-templates   # Groovy 模板               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.10 其他模块

```
┌─────────────────────────────────────────────────────────────────┐
│                        其他模块                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  spring-boot-misc                                               │
│  ├── spring-boot-cache              # 缓存抽象                  │
│  ├── spring-boot-validation         # 验证 (Bean Validation)   │
│  ├── spring-boot-mail               # 邮件发送                  │
│  ├── spring-boot-aop                # AOP 支持                  │
│  ├── spring-boot-docker-compose     # Docker Compose 集成       │
│  ├── spring-boot-testcontainers     # Testcontainers            │
│  └── spring-boot-native             # GraalVM Native Image      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 四、Starter 命名变化

### 4.1 主要变更

| 旧名称 (Boot 3.x) | 新名称 (Boot 4.0) | 说明 |
|------------------|------------------|------|
| `spring-boot-starter-web` | `spring-boot-starter-webmvc` | 更明确的命名 |
| `spring-boot-starter-test` | 各技术 `-test` starter | 分离测试依赖 |
| - | `spring-boot-starter-opentelemetry` | 新增 |
| - | `spring-boot-starter-kotlin-serialization` | 新增 |

### 4.2 Starter 与 Test Starter 配对

```
┌─────────────────────────────────────────────────────────────────┐
│                  Starter 配对规则                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   主 Starter                        测试 Starter                │
│   ─────────────────────────────────────────────────────────────│
│   spring-boot-starter-webmvc   ──►  spring-boot-starter-webmvc-test   │
│   spring-boot-starter-webflux  ──►  spring-boot-starter-webflux-test  │
│   spring-boot-starter-data-jpa ──►  spring-boot-starter-data-jpa-test │
│   spring-boot-starter-batch    ──►  spring-boot-starter-batch-test    │
│   spring-boot-starter-security ──►  spring-boot-starter-security-test │
│   ...                                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 五、迁移支持

### 5.1 经典 Starter (过渡期使用)

```xml
<!-- 保持旧行为，方便渐进式迁移 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-classic</artifactId>
</dependency>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test-classic</artifactId>
    <scope>test</scope>
</dependency>
```

### 5.2 迁移建议

```
┌─────────────────────────────────────────────────────────────────┐
│                        迁移步骤                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 先使用 classic starter 确保应用正常运行                      │
│                                                                 │
│  2. 逐步替换为新的模块化 starter                                 │
│     • 移除 classic starter                                      │
│     • 添加具体技术的 starter                                    │
│     • 添加对应的 test starter                                   │
│                                                                 │
│  3. 检查导入语句                                                │
│     • 部分类可能移动到新包                                      │
│     • IDE 自动修复大部分导入问题                                │
│                                                                 │
│  4. 运行测试确保功能正常                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```
